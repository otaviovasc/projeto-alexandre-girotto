<div class="container py-4">
  <div class="d-flex justify-content-between align-items-center mb-4">
    <div>
      <h1 class="h3 mb-1">Editar e-mail automático</h1>
      <p class="text-muted mb-0"><%= @template.trigger_key %></p>
    </div>
    <%= link_to 'Voltar', admin_reservation_email_templates_path, class: 'btn btn-outline-secondary' %>
  </div>

  <%= render 'form', template: @template %>
</div>
